PORT=8080

# Должен совпадать с RELAY_TOKEN в local-server/.env
RELAY_TOKEN=change-me-shared-with-relay

# MVP: общий токен для веб-клиента. Заменить на нормальную аутентификацию
# пользователей до того, как отдавать доступ кому-то кроме себя.
CLIENT_TOKEN=change-me-client-token
