# Минимальный конфиг для домашней сети.
# allow_anonymous true — приемлемо, т.к. брокер не смотрит наружу
# (наружу выходит только local-server через relay-client.js).
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
