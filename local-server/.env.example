PORT=3000
MQTT_URL=mqtt://mosquitto:1883
DEVICES_FILE=./devices.json

# Токен для доступа к REST/WS API этого сервера из локальной сети (веб-клиент).
# Можно не задавать — сервер сам сгенерирует случайный токен при первом
# запуске, сохранит его на volume (см. docker-compose.yml) и напечатает
# в лог (docker logs domovoy-local-server). Задавай явно только если
# нужно зафиксировать конкретное значение.
# LOCAL_TOKEN=change-me

# Если хотите доступ извне через облачный релей — задайте RELAY_URL.
# Оставьте пустым, чтобы работать только локально.
RELAY_URL=wss://your-domovoy-relay.example.com

# RELAY_TOKEN обычно задавать не нужно — по умолчанию используется тот же
# LOCAL_TOKEN. Это и есть "ключ доступа к дому": впиши его же (Bearer) в
# переменные окружения cloud-relay в Dokploy (RELAY_TOKEN), чтобы облако
# узнавало этот дом и пускало по нему в /admin.
# RELAY_TOKEN=change-me-only-if-you-need-a-different-value
