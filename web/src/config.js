// Известный адрес облачного релея — чтобы не вводить его вручную при первом
// запуске. Если адрес сменится (переезд на другой сервер/домен) — правим
// только эту строку; тот, кто уже сохранил свой адрес в настройках (⚙),
// это значение не переопределит.
export const DEFAULT_CLOUD_URL = 'https://domovoy-rmwgyh-b28a81-185-244-48-218.sslip.io';

// Известный адрес локального сервера в домашней сети (IP самого устройства,
// на котором крутится local-server, — NAS/мини-ПК). Он у нас один и не
// меняется, поэтому вписывать его руками незачем. Сменится оборудование
// или IP — правим только эту строку.
export const DEFAULT_LOCAL_URL = 'http://192.168.0.5:3000';
